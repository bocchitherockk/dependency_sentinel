[project]
name = "dependency-sentinel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.sources]
repository-storage-service = { workspace = true }

[tool.uv.workspace]
members = [
    "services/*",
    "libs/*"
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
